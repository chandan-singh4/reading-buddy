# Copy this file to .env and fill in the real value.
# .env itself is gitignored — it never gets committed. This file (.env.example)
# is the only one that is, so anyone setting up the project knows what to fill in.

ANTHROPIC_API_KEY=
